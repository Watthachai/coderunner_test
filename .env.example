# PostgreSQL connection string used by Prisma (server-only — never NEXT_PUBLIC_).
# Copy this file to `.env` and adjust the credentials for your local database.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cafe_preorder?schema=public"
